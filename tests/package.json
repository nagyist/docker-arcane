{
	"name": "arcane-tests",
	"type": "module",
	"scripts": {
		"preinstall": "npx only-allow pnpm",
		"check": "tsc --noEmit",
		"format": "oxfmt",
		"format:check": "oxfmt --check",
		"test": "playwright test"
	},
	"devDependencies": {
		"@playwright/test": "^1.62.1",
		"@types/node": "^26.1.2",
		"oxfmt": "^0.61.0",
		"typescript": "^7.0.2"
	}
}
